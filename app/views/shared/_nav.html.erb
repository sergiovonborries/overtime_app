<div class="logo">
  <h2>Overtime</h2>
</div>
<% if current_user %>
  <ul class="custom-nav nav nav-tabs">
    <li class="nav-item">
      <%= link_to "Inicio", root_path, class: "nav-link #{active?(root_path)}"%>
    </li>
    <li class="nav-item">
      <%= link_to "Posts", posts_path, class: "nav-link #{active?(posts_path)}"%>
    </li>
    <%= render 'shared/audit_log_tab' if policy(AuditLog).index? %>

    <li class="nav-item">
      <%= link_to "New Entrie", new_post_path, id: "new_post_from_nav", class: "nav-link #{active?(new_post_path)}"%>
    </li>
    <li class="nav-item dropdown ml-md-auto">
       <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown">Perfil</a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
        <% if current_user.try(:type) == "AdminUser" %>
          <%= link_to "Admin Dashboard", admin_root_path, class: "dropdown-item" %>
         <div class="dropdown-divider">
         </div>
        <% end %>
          <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>
    </li>
  </ul>
<% end %>
